{
  "name": "osint-api-worker",
  "version": "1.0.0",
  "description": "Custom OSINT API with credit system deployed on Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "build": "wrangler build",
    "backend": "node dev-backend.js",
    "test": "node test-api.js",
    "demo": "node full-demo.js",
    "setup": "node quick-start.js",
    "start": "npm run dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251230.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.54.0"
  }
}
